export interface IAddress {
  address: string; // k-81, arihant arden
  landmark: string; // near bishrakh
  city: string; // greater noida west
  state: string; // up
  pincode: string; // 201306
  location: {
    lat: string;
    lng: string;
  };
}
